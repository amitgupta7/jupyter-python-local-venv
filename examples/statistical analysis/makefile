fileName=ApplianceTelemetryCorrelationAnalysis.ipynb
nbupdate:
	jupyter nbconvert --execute --to notebook --inplace ${fileName}
	jupyter nbconvert --to python ${fileName}
	jupyter nbconvert --to html ${fileName}
	jupyter nbconvert --to pdf ${fileName}
